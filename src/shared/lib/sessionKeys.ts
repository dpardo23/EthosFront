/**
 * Centralized session storage keys for authentication tokens.
 * This module ensures consistent key names across all parts of the auth system.
 */

export const ACCESS_TOKEN_KEY = 'ethoshub_access_token';
export const TOKEN_TYPE_KEY = 'ethoshub_token_type';
export const EXPIRES_AT_KEY = 'ethoshub_access_expires_at';
